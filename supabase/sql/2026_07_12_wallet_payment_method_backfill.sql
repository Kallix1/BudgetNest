-- ============================================================
-- BudgetNest — поправка на "изгубени" пари в портфейла
-- Дата: 2026-07-12
--
-- Проблем: транзакциите, създадени ПРЕДИ въвеждането на
-- портфейла (карта / в брой), имат payment_method = NULL.
-- Изгледът wallet_balances брои само 'card' и 'cash', затова
-- тези суми влизат в "Приходи/Разходи този месец", но липсват
-- от "В картата", "В брой" и "Общо".
--
-- Изпълнете това в Supabase → SQL Editor.
-- ============================================================

-- 1) Еднократно: старите транзакции без метод стават "карта".
--    (Същото подразбиране, което ползва и формата за редакция.)
--    Ако някоя от тях реално е била в брой, потребителят може
--    после да я редактира от сайта и да смени метода.
UPDATE public.transactions
SET payment_method = 'card'
WHERE payment_method IS NULL;

-- 2) Защита занапред: колоната получава стойност по подразбиране,
--    така че нов запис без изрично зададен метод да не "изчезва".
ALTER TABLE public.transactions
ALTER COLUMN payment_method SET DEFAULT 'card';

-- 3) (По желание, след като 1) е минало успешно) — забрана на NULL,
--    за да не може бъгът да се върне от нов код:
-- ALTER TABLE public.transactions
-- ALTER COLUMN payment_method SET NOT NULL;

-- ============================================================
-- Допълнителни проверки (ръчно):
--
-- А) Функцията run_recurring_now (RPC, вика се от dashboard.html):
--    проверете в Supabase → Database → Functions, че когато тя
--    създава транзакция от периодично правило, копира
--    recurring_transactions.payment_method в transactions.
--    Ако не го прави — това е още един източник на NULL.
--
-- Б) За пълна защита изгледът wallet_balances може да третира
--    NULL като 'card' чрез COALESCE(payment_method, 'card').
--    Дефиницията му не е в репото, затова не я променяме тук —
--    вижте я в Supabase → Database → Views и при нужда добавете
--    COALESCE на местата, където се сравнява payment_method.
-- ============================================================
